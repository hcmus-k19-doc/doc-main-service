spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:postgresql://john.db.elephantsql.com:5432/chqjllyd
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://host.docker.internal:8180/realms/doc-realm

  rabbitmq:
    host: 13.215.56.216
    port: 5672
    username: guest
    password: guest
    template:
      exchange: doc.exchange
      routing-key: doc.routingkey
      attachment-routing-key: doc.attachment-routing-key

  main:
    allow-circular-references: true

  liquibase:
    url: jdbc:postgresql://john.db.elephantsql.com:5432/chqjllyd
    user: chqjllyd
    password: NKOViHVWaKSbfXls3XfY2LMr4kftmUCq
    driver-class-name: org.postgresql.Driver
    change-log: classpath:db.changelog-master.xml
    database-change-log-lock-table: database_changelog_lock
    database-change-log-table: database_changelog
    enabled: true
    drop-first: false

logging:
  level:
    sql: debug
    web: debug

doc:
  http:
    auth-token-header-name: DOC-Auth-Token
    auth-token: secret

keycloak:
  url: http://host.docker.internal:8180/realms/doc-realm/protocol/openid-connect
  scope: openid
  client-id: doc-client
  client-secret: AW4aUnOGcPpitHX0TI6grfZeA3WLt5ru

management:
  endpoint:
    health:
      probes:
        enabled: true
